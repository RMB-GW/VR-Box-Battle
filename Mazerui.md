## 2026.03.27 —— 随机箱子生成系统（BoxSpawner）

- 编写 `BoxSpawner.cs` 脚本，实现**区域随机箱子生成**功能
- 支持两种定位方式：优先使用 `Collider.bounds` 划定生成范围，未指定时回退到 `areaCenter` 的相对偏移
- 生成位置在 X/Z 平面完全随机，Y 轴高度范围可独立配置
- 集成 **碰撞检测机制**：利用 `Physics.CheckBox()` 在生成前检测是否与其他物体重叠
- 自动计算预制体 BoxCollider 的半边长（考虑世界缩放），保证碰撞检测精度
- 单次生成最多尝试 50 次，避免死循环
- 每个箱子赋予 **随机 Y 轴旋转**（0°~360°），增加视觉多样性

## 2026.04.03 —— 随机箱子系统场景集成

- 将 `BoxSpawner` 挂载到场景物体上，配置生成区域、数量、层级等参数
- 调试并验证碰撞检测在不同地形和障碍物环境下的表现
- 调整生成范围与高度参数，适配场景布局

![alt text](a.png)
![alt text](b.png)


## 2026.04.10 —— 机器人平衡控制系统

- 编写 `RobotBalanceController.cs`，实现完整的**双闭环平衡控制**算法

### 上半身（躯干俯仰）—— PID 控制
- **比例项 Kp=20.0**：主导平衡校正
- **积分项 Ki=1.2**：消除稳态误差，带积分限幅 ±20°
- **微分项 Kd=2.5**：抑制震荡、提供阻尼
- 输出限幅 ±18°，保证关节运动在安全范围

### 下半身（底座直立）—— PD 控制
- **比例项 Kp=12.0，微分项 Kd=1.5**，输出限幅 ±12°
- **动态权重融合**：
  - 基础权重 `baseControlWeight = 1.8`
  - 误差越大权重越大：`权重 += |误差| × 0.08`
  - 自适应调节上下半身控制的贡献比例

### 三关节加权输出
- 将融合后的控制量按权重（1.0、0.8、0.6）和符号分配到 3 个俯仰关节
- 每个关节目标值经 `ArticulationBody.xDrive.lowerLimit/upperLimit` 限幅

### 防震荡与信号处理
- **低通滤波**（8Hz 截止频率）：滤除躯干/底座角速度噪声
- **Slew Rate 限幅**（60°/s）：平滑输出变化率，防止突变
- **Anti-Windup**：当输出饱和且误差方向一致时冻结积分累加

### 安全与验证机制
- **30 秒站立验证序列**：启动后先保持直立，验证通过后才切换到小幅前倾目标
- **倾倒检测**：躯干俯仰超过 30° 标记为跌倒，停止控制
- **输入处理**：验证通过后响应垂直方向输入，最大前倾 ±3°

### 辅助脚本
- **`RobotBaseController.cs`**（机器人移动控制）：带加减速平滑的移动/旋转控制，支持力作用点偏移与刹车扭矩软化
- **`RobotMassCenter.cs`**（重心调整）：延迟设置关节重心偏移，兼容 URDF Importer 初始化时序

![alt text](c.png)

## 2026.05.09 —— 传送带箱子生成系统（ConveyorBoxSpawner）

- 编写 `ConveyorBoxSpawner.cs`，实现**智能定时箱子生成**

### 核心逻辑
- 以随机间隔（0 ~ `maxInterval` 秒）在传送带区域生成箱子
- **区域感知**：通过 Trigger Collider 实时追踪区域内的箱子数量
- **智能节流**：区域内已有箱子时不生成，避免拥堵
- **即时补货**：区域内箱子全部离开后立即安排下一次生成，提高效率
- 单次生成最多尝试 50 次防止重叠

![alt text](d.png)

## BUG 修复 —— 2026.05.09

### BUG1：ScoreManager 循环调用崩溃

**问题**：从 Game 场景切换到 Easy 场景时，`ScoreManager.cs` 中 `RefreshScoreText()` 与 `ShowScore()` 构成**循环递归调用**，导致 Unity 卡死并抛出 `NullReferenceException`。

**根因分析**：`RefreshScoreText()` 内部调用了 `ShowScore()`，而 `ShowScore()` 又调用了 `RefreshScoreText()`，形成无限递归栈溢出。

**修复方案**：重构 `ScoreManager.cs`，将 `RefreshScoreText()` 中的循环调用移除，改为仅刷新 UI 文本显示；`ShowScore()` 负责激活 UI 对象并仅调用一次 `RefreshScoreText()` 更新显示内容。职责分离后，两个方法单向调用，彻底消除循环递归风险。

